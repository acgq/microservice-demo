spring:
  application:
    name: licensingservice

  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active:
      default
#Config:
#  profiles:
#    active:
#      default
server:
  port: 8081
